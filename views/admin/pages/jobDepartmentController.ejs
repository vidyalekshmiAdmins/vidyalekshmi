<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eHYk0b9676Q126zY2K4141975658000829472925163294709283344275601037" crossorigin="anonymous">
</head>
<style>
  body {
    background-color: #f5f5f5;
  }
  .container {
    margin-top: 50px;
  }
  h1 {
    color: #333;
    text-align: center;
  }
  .list-group-item {
    background-color: #fff;
    border: none;
    padding: 15px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  }
  .list-group-item a {
    color: #333;
    text-decoration: none;
  }
  .list-group-item a:hover {
    text-decoration: underline;
  }
  .btn-primary {
    margin-top: 20px;
    background-color: #007bff;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    font-weight: bold;
  }
  .btn-primary:hover {
    background-color: #0069d9;
  }
</style>
<body>

 
  <div class="container">
    <h1>STAFF DEPARTMENT<br>Management</h1>

    <% if (successMessage) { %>
      <div class="alert alert-success"><%= successMessage %></div>
    <% } %>
    <% if (errorMessage) { %>
      <div class="alert alert-danger"><%= errorMessage %></div>
    <% } %>
    <br><br>

    <h2>Available Departments</h2>
    <ul class="list-group">
      <% if (sectionList.length > 0) { %>
        <% sectionList.forEach(section => { %>
          <li class="list-group-item">
            <a href="/admin/staff-management/<%= section._id %>"><%= section.sectionName %></a> </li>
        <% }); %>
      <% } else { %>
        <li class="list-group-item">No sections available.</li>
      <% } %>
    </ul>

    <button type="button" class="btn btn-primary" onclick="window.location.href='/admin/sections-management'">Add Job Department</button>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+89V76pHOis/o" crossorigin="anonymous"></script>
</body>
</html>
